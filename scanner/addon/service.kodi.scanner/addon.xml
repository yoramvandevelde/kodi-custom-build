<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="service.kodi.scanner"
       name="Library Scanner"
       version="1.0.0"
       provider-name="kodi-custom-build">
  <requires>
    <import addon="xbmc.python" version="3.0.0"/>
  </requires>
  <extension point="xbmc.service" library="service.py"/>
  <extension point="xbmc.addon.metadata">
    <summary lang="en_GB">Scan the video library, then quit</summary>
    <description lang="en_GB">Runs a video library update followed by a clean, waits for both to report finished, and then shuts Kodi down. Exists so a headless container can do one library pass per boot without anything needing to poll it from outside.</description>
    <platform>all</platform>
    <license>GPL-2.0-only</license>
  </extension>
</addon>
